export default (theme) => ({
  cardFooter: {
    padding: '10px',
    margin: '0 15px 10px',
    justifyContent: 'flex-end',
    alignItems: 'center',
    display: 'flex',
    borderTop: `1px solid ${theme.palette.divider}`
  }
});
